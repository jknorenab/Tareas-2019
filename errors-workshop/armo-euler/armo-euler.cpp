#include <cstdio>
#include <cmath>

int main (void){

  //Armamos la función, armonica, tal como lo habiamos hecho antes, y ahora le restamos ln(x) La cosa está en imprimir valores intermedios cada 100 pasos (cómo?)

  return 0;
}

